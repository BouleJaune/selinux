inverser slides 
audit2allow (générer une règle) et restorecon (gestion des contextes de fichiers)
chcon présent dans troubleshoot 

sesearch -s bin_t -A

sesearch exemple y a pas de var_log_t, juste des httpd_log_t 1st slide avancée

setfiles / fixfiles que font-elles


TODO : python flask pertinence


check quoi est présent par défaut:
  118  dnf install seinfo
  119  dnf install setools-console
  147   dnf install seztroubleshoot-server
  335  dnf install httpd

policycoreutils-python-utils par défaut ?


TP2 : 
- semanage fcontext + restorecon sur l'exemple du TP1, fcontext à mettre en absolu


httpd/apache : sur le port 8080, fichier index.html dans un dossier random /web
fcontext port, fcontext dossier



sealert -l id
id trouvable dans var log messages | grep AVC -C 4
getsebool -a 


TP1: troubleshooting
- découverte fedora
- découverte enforcing, politique etc
- premier troubleshooting avec myscript service
- logs, ausearch, sealert
- solution, idée, chcon, sesearch lister les droits sur type

TP2: gestion de politique
- installation d'apache
- Déplacer le dossier web dans /webdata => blocage
- analyse logs, quel devrait être le bon contexte
- semanage + restorecon pour solution non-temporaire
- utilisation de booléen : exec de script par apache into activer booléen httpd_can_network_connect

TP3:
- reprendre le service myscript
- restorecon
- faire un module custom à partir de ça
- l'observer

TP4: 
MLS/MCS

TP5: 
Container

CIL language
q